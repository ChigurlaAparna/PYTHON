lst=list(map(int,input("Enter the list:").split()))
visited=[]
for i in lst:
    if i not in visited:
        count=0
        for j in lst:
            if i==j:
                count+=1
        print(i," ",count)   
        visited.append(i)             
